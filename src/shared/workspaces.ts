export const DEFAULT_WORKSPACE_KEY = "default";
export const DEFAULT_WORKSPACE_NAME = "Default Workspace";
// Stable so migrations and runtime compatibility code can seed/reference the same record.
export const DEFAULT_WORKSPACE_ID = "workspace_default";
