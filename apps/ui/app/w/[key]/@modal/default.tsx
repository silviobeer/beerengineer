/**
 * Empty default for the `modal` parallel slot. Rendered when no
 * intercepting route is active — i.e. when the user is just on the
 * board and not viewing an item.
 */
export default function ModalDefault(): null {
  return null;
}
